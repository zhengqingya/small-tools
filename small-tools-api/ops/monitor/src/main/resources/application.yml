# http://localhost:30000/actuator
server:
  port: 30000

spring:
  security:
    user:
      name: admin
      password: 123456
  boot:
    admin:
      ui:
        title: small-tools服务状态监控

# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*
'