server:
  port: 10010 # 服务端口
spring:
  application:
    name: ums # 应用名称
  profiles:
    active: dev # 环境配置

small-tools:
  mysql:
    master:
      db-name: small-tools-mall # 主库


# 微信小程序
wx:
  miniapp:
    appid: xxx
    secret: xxx
    msgDataFormat: JSON
